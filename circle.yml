test:
  override:
    - FAKE=faking
deployment:
  production:
    branch: master
    commands:
      - curl -X POST https://api.newrelic.com/v2/applications/123/deployments.json -v --trace-ascii /dev/stdout
